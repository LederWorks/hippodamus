version: "1.0"
metadata:
  title: "Invalid Config - Project without Organization"
  description: "This should fail dependency validation"
  author: "Dependency Test"
  tags: ["test", "invalid", "dependency-validation"]

templates:
  - name: "azuredevops-project"
    template: "azuredevops/azuredevops-project.yaml"

diagram:
  pages:
    - id: "TEST"
      name: "Invalid Dependency Test"

      elements:
        # This should FAIL - project without organization parent
        - type: "template"
          id: "orphan-project"
          name: "Orphan Project"
          template: "azuredevops-project"
